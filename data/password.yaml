# MagicHub 访问密码配置
# 修改后 30 秒内自动生效

# 访问密码（明文）；留空则不启用密码保护
password: "passward"

# 受保护的文件/目录路径（相对于 data/）
# 支持精确匹配和前缀匹配
# 标记为 protected 的文件：
#   - Web UI：未登录时显示锁定图标和脱敏内容
#   - Raw URL：需要 ?key=JWT_token 参数才能访问，否则返回 403
protected:
  # Surge 配置
  - path: "Proxy/Surge/macOS.conf"
  - path: "Proxy/Surge/iOS.conf"
  - path: "Proxy/Surge/Macmini.conf"
  - path: "Proxy/Surge/nodes"
  # Mihomo/Clash 配置
  - path: "Proxy/Clash/config.yaml"
  - path: "Proxy/Clash/config-nas.yaml"
  - path: "Proxy/Clash/config-android.yaml"